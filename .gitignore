# Local .terraform directories
.terraform/

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version 
# control as they are data points which are potentially sensitive and subject 
# to change depending on the environment.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore transient lock info files created by terraform apply
.terraform.tfstate.lock.info

# Include override files you do wish to add to version control using negated pattern
# !example_override.tf

# Plan files from deploy lifecycle (terraform plan -out=tfplan)
*tfplan*

# Account-specific auto-vars generated by deploy-init.sh
projects/**/.auto.tfvars.json

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Environment files (secrets — NEVER commit)
.env
.env.local
.env.*.local

# ADLC Evidence (ephemeral)
tmp/

# Workspace cache (terraform plugins, trivy, go modules)
.cache/

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db

# Infracost
.infracost/
.checkov/custom_checks/__pycache__

.terraform.lock.hcl

# Web Module TODOs

Tracked here (not as INVEST stories) — small enhancements, not story-worthy.

- [ ] **P2**: Add `AWSManagedRulesSQLiRuleSet` + `AWSManagedRulesAmazonIpReputationList` to both WAF scopes ($0 cost)
- [ ] **P3**: Add `AWSManagedRulesBotControlRuleSet` to CloudFront WAF ($10/mo — currently REGIONAL-only)
- [ ] **P2**: WAF request-level logging (`CKV2_AWS_31`) — deferred to M5 Observability module (APRA CPS 234 s.14b gap)

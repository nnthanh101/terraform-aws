# Terraform Cloud / HCP Terraform — Registry Publication
# Copy to .env and fill values. NEVER commit .env (see .gitignore).
TFE_TOKEN=
TF_CLOUD_ORGANIZATION=oceansoft
TF_CLOUD_HOSTNAME=app.terraform.io

# terraform-aws environment configuration
# Copy to .env and customize: cp .env.example .env

# AWS Profile (must match ~/.aws/config entry)
AWS_PROFILE=terraform-aws

# AWS Region (default: ap-southeast-2)
AWS_REGION=ap-southeast-2

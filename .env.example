# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# terraform-aws — Environment Configuration
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Copy to .env and fill values. NEVER commit .env (see .gitignore).
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# ── AWS Configuration ─────────────────────────────────────────────────────────────
# Required for: Tier 3 tests (real AWS), live deployments
# Auth: aws sso login --profile=<your-profile>
AWS_PROFILE=
AWS_REGION=ap-southeast-2

# ── HCP Terraform / Terraform Enterprise ──────────────────────────────────────────
# Required for: terraform login, awslabs-terraform MCP server, API-driven operations
# NOT required for: VCS-driven registry publish (uses GitHub OAuth at app.terraform.io)
# Scope needed: "Manage modules" for registry publish, "Manage workspaces" for remote ops
# Generate at: https://app.terraform.io/app/settings/tokens
# Optional for local development if only using VCS-driven publish flow
TFE_TOKEN=
TF_CLOUD_ORGANIZATION=oceansoft
TF_CLOUD_HOSTNAME=app.terraform.io

# ── Infracost (FinOps) ───────────────────────────────────────────────────────────────────
# Required for: task plan:cost, infracost.yml CI workflow
# Get API key at: https://www.infracost.io/docs/#2-get-api-key
INFRACOST_API_KEY=
